## Step 1
前回まで使っていたListNodeを使えば解けるか。
```python
class ListNode:
        def __init__(self, val=None, next=None):
            self.val = val
            self.next = next

class Solution:
    def isValid(self, s: str) -> bool:
        close_to_open = {
            ")": "(",
            "}": "{",
            "]": "[",
        }
        i = 0
        dummy = ListNode(None)
        bracket_log = dummy
        while i < len(s):
            j = 0
            # creat a open bracket log
            while i+j < len(s):
                bracket = s[i+j]
                if bracket in close_to_open:
                    break
                bracket_log = ListNode(bracket, bracket_log)
                j += 1
            if i+j == len(s) or bracket_log.val is None:
                return False
            # validate correspondence 
            while bracket_log.val is not None:
                if i+j == len(s) and bracket_log.val != None:
                    return False
                bracket = s[i+j]
                if bracket not in close_to_open:
                    break
                if bracket_log.val != close_to_open[bracket]:
                    return False
                bracket_log = bracket_log.next
                j += 1
            i += j
        return True
```
時間計算量: O(n)<br>
実行時間: 10^4 / 10^{-6} = 10^{-2}<br>
空間計算量: O(n)<br><br>

なんか問題に対してコードがごちゃついてるな？　整理する。
```python
class ListNode:
        def __init__(self, val=None, next=None):
            self.val = val
            self.next = next

class Solution:
    def isValid(self, s: str) -> bool:
        close_to_open = {
            ")": "(",
            "}": "{",
            "]": "[",
        }
        open_brackets = ListNode()
        i = 0
        while i < len(s):
            bracket = s[i]
            if bracket not in close_to_open:
                open_brackets = ListNode(bracket, open_brackets)
            else:
                if open_brackets.val is None or close_to_open[bracket] != open_brackets.val:
                    return False
                open_brackets = open_brackets.next
            i += 1
        return open_brackets.val is None # whether s ends with a close bracket
```
あれ？　ListNodeである必要なくないか？ List.popでうまくいく気がする。
```python
class Solution:
    def isValid(self, s: str) -> bool:
        close_to_open = {
            ")": "(",
            "}": "{",
            "]": "[",
        }
        open_brackets = []
        i = 0
        while i < len(s):
            bracket = s[i]
            if bracket not in close_to_open:
                open_brackets.append(bracket)
            else:
                if not open_brackets or close_to_open[bracket] != open_brackets[-1]:
                    return False
                open_brackets.pop(-1)
            i += 1
        if open_brackets:
            return False
        return True
```
うまくいった。
## Step 2
**PRを読む**<br>
[https://github.com/katataku/leetcode/pull]/6(https://github.com/katataku/leetcode/pull/6)
[https://github.com/tarinaihitori/leetcode/pull/7](https://github.com/tarinaihitori/leetcode/pull/7)
[https://github.com/h1rosaka/arai60/pull/8](https://github.com/h1rosaka/arai60/pull/8)
[https://github.com/hayashi-ay/leetcode/pull/16](https://github.com/hayashi-ay/leetcode/pull/16)
[https://github.com/seal-azarashi/leetcode/pull/6](https://github.com/seal-azarashi/leetcode/pull/6)

> open, closeの代わりにleft, rightも選択肢ですかね。
> [https://github.com/katataku/leetcode/pull/6#discussion_r1847773308](https://github.com/katataku/leetcode/pull/6#discussion_r1847773308)
知らなかった。

> これの一つ辛いところは stack.append(char) を読んだ後に次の行がどこかを探すために、7行下まで見て次がないことを確認するために目を動かさないといけないことです。
なので、次の行に continue を入れて切ってしまうのは結構有力な手です。
> [https://github.com/tarinaihitori/leetcode/pull/7#discussion_r1812923237](https://github.com/tarinaihitori/leetcode/pull/7#discussion_r1812923237)
なるほど。自分も入れよう。

> valid parentheses は、チョムスキー階層、タイプ-2、文脈自由文法だから、プッシュダウンオートマトンで書ける、を多分連想します
> [https://discord.com/channels/1084280443945353267/1201211204547383386/1202541275115425822](https://discord.com/channels/1084280443945353267/1201211204547383386/1202541275115425822)
チョムスキー階層、タイプ-2、文脈自由文法だから、プッシュダウンオートマトンの全てを知らなかった。
次の資料に目を通した。
- [茨城大学 宮本さんの講義資料](https://miyamoto.cis.ibaraki.ac.jp/class_abst.html)
- [静岡大学 尾崎さんの講義資料](https://lab.inf.shizuoka.ac.jp/masakiowari/secret/automaton2.pdf)
- [CZAPでの小西さんの資料](https://discord.com/channels/1084280443945353267/1394676785194733619/1413443095181266945)
生成文法かなり面白い。ひとまず押さえておくべきは、
1. 文字列の生成規則には表現力の階層がある（正規 < 文脈自由 < 文脈依存 < 無制限; それぞれの間に他のもある）
2. 文字列を状態に付されたラベルだと思えば、生成規則は状態の遷移である
3. 逆に状態の遷移からそれと同等な生成規則もある。
4. 状態の遷移が正規文法で言い換えられるとき、受理・不受理の処理、つまり与えられた状態の遷移に入力がしたがっているかの判断には、決定性有限オートマトンを用いれば良い。
5. 状態の遷移が文脈自由文法で言い換えられるとき、受理・不受理の処理にはプッシュダウン・オートマトンを用いれば良い。
だろうか？

**コメント集を読む**<br>
[https://docs.google.com/document/d/11HV35ADPo9QxJOpJQ24FcZvtvioli770WWdZZDaLOfg/edit?tab=t.0#heading=h.ns0bie22a6m](https://docs.google.com/document/d/11HV35ADPo9QxJOpJQ24FcZvtvioli770WWdZZDaLOfg/edit?tab=t.0#heading=h.ns0bie22a6m)<br>

> deque は peek ができます。[0]で先頭、[-1]で最後尾にアクセスできます。
> [https://discord.com/channels/1084280443945353267/1225849404037009609/1231646037077131315](https://discord.com/channels/1084280443945353267/1225849404037009609/1231646037077131315)

dequeタイプ初めて知った。そりゃあるか。でもユースケースとしては、listほど自由に操作したくない／操作されたくないという場合だろうか？<br>

> "(aiu)[eo]" が入力としてきたときに、プログラムの挙動として好ましいのは何だと考えますか?
> [https://discord.com/channels/1084280443945353267/1225849404037009609/1231648833914802267](https://discord.com/channels/1084280443945353267/1225849404037009609/1231648833914802267)

この場合はopening bracketの判定が必要なので以下のようにするのがいいかも。<br>
先に書いたコードと比べると、close_to_openをopen_to_closeにした。その方が何だか決まりがいい。<br>
```python
class Solution:
    def isValid(self, s: str) -> bool:
        open_to_close = {
            "(": ")",
            "{": "}",
            "[": "]",
            "*": ""
        }
        stack = ["*"]
        for c in s:
            if c in open_to_close:
                stack.append(c)
            if c in open_to_close.values():
                if c != open_to_close[stack[-1]]:
                    return False
                else:
                    stack.pop()
        return stack == ["*"]
```
ただ、問題では`a string s containing just the characters '(', ')', '{', '}', '[' and ']'`と指定されているから、上のコードをこの問題の回答として提出するのは不適切な気がする。括弧以外の文字は異常な入力なので。<br>

> 個人的には副作用のある式を条件のところにあまり書きたくないですね。
> [https://github.com/yus-yus/leetcode/pull/6#discussion_r1944970090](https://github.com/yus-yus/leetcode/pull/6#discussion_r1944970090)

ふむ。dequeのpopは[要素がひとつも存在しない場合はIndexErrorを発生させる](https://docs.python.org/ja/3.13/library/collections.html#collections.deque:~:text=%E3%82%92%E8%BF%94%E3%81%97%E3%81%BE%E3%81%99%E3%80%82-,%E8%A6%81%E7%B4%A0%E3%81%8C%E3%81%B2%E3%81%A8%E3%81%A4%E3%82%82%E5%AD%98%E5%9C%A8%E3%81%97%E3%81%AA%E3%81%84%E5%A0%B4%E5%90%88%E3%81%AF%20IndexError%20%E3%82%92%E7%99%BA%E7%94%9F%E3%81%95%E3%81%9B%E3%81%BE%E3%81%99%E3%80%82,-popleft())ので、ifの条件式の中に入れると思考を分岐させなければならず辛い。

## Step 3
```python
class Solution:
    def isValid(self, s: str) -> bool:
        open_to_close = {
            "(": ")",
            "{": "}",
            "[": "]"
        }
        bracket_cache = []
        for bracket in s:
            if bracket in open_to_close:
                bracket_cache.append(open_to_close[bracket])
                continue
            elif bracket_cache and bracket == bracket_cache[-1]:
                bracket_cache.pop()
                continue
            else:
                return False
        return not bracket_cache
```

## 感想
習慣が途切れてしまった。またやっていきたい。
12/14の忘年会までに終わらせることを目標とする。
