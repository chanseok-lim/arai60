## Step 1
まずは思いついたまま書く
```python
class KthLargest:

    def __init__(self, k: int, nums: List[int]):
        self.k = k
        self.stream = sorted(nums)
        

    def add(self, val: int) -> int:
        self.stream.append(val)
        self.stream.sort()
        return self.stream[-1*self.k]
```
Submitした結果、なんかめっちゃ遅いっぽい。addするたびにsortするからだと思うが。。。もう少し考えてみる。<br>
ソートするのはTop kだけでよいので、top_kというlistを作ったみる。
```python
class KthLargest:

    def __init__(self, k: int, nums: List[int]):
        self.k = k
        self.stream = sorted(nums) 
        self.top_k = self.initialize_top_k()
        self.kth_largest = self.top_k[-1]


    def initialize_top_k(self):
        if len(self.stream) >= self.k:
            top_k = [self.stream[-1 * i] for i in range(1, self.k + 1)]
            return top_k
        else:
            top_k = []
            top_k.extend(self.stream)
            shortage = self.k - len(self.stream)
            dummy = [-1 * float('inf') for i in range(shortage)]
            top_k.extend(dummy)
            return sorted(top_k, reverse=True)


    def add(self, val: int) -> int:
        if self.kth_largest > val:
            self.stream.append(val)
            return self.kth_largest
        else:
            self.top_k.pop()
            self.top_k.append(val)
            self.top_k.sort(reverse=True)
            self.kth_largest = self.top_k[-1]
        return  self.kth_largest
```
テストをクリアするが、まだ遅い。。。これ以上はパッと思いつかないので、Step 2に進む。

## Step 2

## Memo
heap 知らなかった。
heap := an array visualized as a tree
木構造かつ左が右よりもpriorityが大きいという制約を入れて持っておけば、上からk番目をソートせずに持って来れるし、新しいelementが入ってきた時のpriorityの計算がすごく簡単。なぜなら、treeの各階層の右端（もしくは左端）のみをまず見て、その後その階層を探索することで入れるべき場所がわかる。また後ろのkeyを1ずらすだけで良い。
