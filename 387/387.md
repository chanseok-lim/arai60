## Step 1
とりあえず思いつくままに書く。
```python
class Solution:
    def filter_first_unique(
        self, char_to_indeces: Dict[str, List[int]]
    ) -> (str, int):
        unique_chars = []
        for c, inds in char_to_indeces.items():
            if len(inds) == 1:
                unique_chars.append((c, *inds))
        unique_chars = sorted(unique_chars, key=lambda x: x[1])

        if not unique_chars:
            return (None, -1)
        else:
            return unique_chars[0]

    def firstUniqChar(self, s: str) -> int:
        char_to_indeces = defaultdict(list)
        for i, c in enumerate(s):
            char_to_indeces[c].append(i)
        _, i = self.filter_first_unique(char_to_indeces)
        
        return i
```
時間計算量：<br>
char_to_indecesの作成 + unique_charsの作成 + sorted<br>
= n + n + mlogm = 2n + mlogm = (2+rlogr+rlogn)n, 0 < r < 1<br>
O(nlogn)<br>
実行時間：10^5 * 5 / 10^6 = 5 * 10^{-1}<br>
lowercase letterに対するチェック忘れてた。
空間計算量：O(n^2)?<br><br>

公式の解答を見る。
```python
class Solution:
    def firstUniqChar(self, s: str) -> int:
        """
        :type s: str
        :rtype: int
        """
        # build hash map: character and how often it appears
        count = collections.Counter(s)
        
        # find the index
        for idx, ch in enumerate(s):
            if count[ch] == 1:
                return idx     
        return -1
```
`collections.Counter`知らなかった。<br>
>[https://docs.python.org/3/library/collections.html#collections.Counter](https://docs.python.org/3/library/collections.html#collections.Counter)

あーそうか。dictは並びが保存されるようになったんだった。<br>
>[the insertion-order preservation nature of dict objects has been declared to be an official part of the Python language spec.](https://docs.python.org/3.13/whatsnew/3.7.html#:~:text=the%20insertion%2Dorder%20preservation%20nature%20of%20dict%20objects%20has%20been%20declared%20to%20be%20an%20official%20part%20of%20the%20Python%20language%20spec.)

ついでにdictの比較についてのdocを漁る<br>
>[Dictionaries compare equal if and only if they have the same (key, value) pairs (regardless of ordering). Order comparisons (‘<’, ‘<=’, ‘>=’, ‘>’) raise TypeError.](https://docs.python.org/3/library/stdtypes.html#mapping-types-dict:~:text=Dictionaries%20compare%20equal%20if%20and%20only%20if%20they%20have%20the%20same%20(key%2C%20value)%20pairs%20(regardless%20of%20ordering).%20Order%20comparisons%20(%E2%80%98%3C%E2%80%99%2C%20%E2%80%98%3C%3D%E2%80%99%2C%20%E2%80%98%3E%3D%E2%80%99%2C%20%E2%80%98%3E%E2%80%99)%20raise%20TypeError.)

## Step 2

## Step 3

## 感想
