## Step 1
初めてみる問題。何が入力として与えられているのかわからず、どんな操作が期待されているかわからない。
`next`と`pos`属性を持ったlist-like objectが渡されるのか？とも思うがギブアップ。<br>
コメント集やDiscodeから解答を探す。<br>
探しながらここ
```
# Definition for singly-linked list.
# class ListNode:
#     def __init__(self, x):
#         self.val = x
#         self.next = None
```
に定義が書かれていること、型ヒントからこの`ListNode`が入力になることに気づく。見えてなさすぎる。<br>
うさぎと亀というアルゴリズムがあるらしいが、とりあえず自分で一回考えてみる。
```python
# Definition for singly-linked list.
# class ListNode:
#     def __init__(self, x):
#         self.val = x
#         self.next = None

class Solution:
    def hasCycle(self, head: Optional[ListNode]) -> bool:
        if head is None:
            return False

        visited_sequence = []
        for i in range(10**4):
            head = head.next

            if head is None:
                return False
            
            if head in visited_sequence:
                return True
            
            visited_sequence.append(head)

        return False
```
時間計算量：O(n^2), n = length of cycle<br>
$\because$ ループごとにlen(visited_sequence)が +1 されてそれを走査する<br>
実行時間：(10^4)^2 / 10^6 = 10^2<br>
空間計算量：O(n)<br>
そういえばTLEって何秒からTLEなんだろうか。<br>
引数のheadを更新してしまっているが、読み返すと嫌な感じがする。書いたときはオートマトン的なイメージがぼんやりあって、それに変数名が引きずられたと思われる。

[https://discord.com/channels/1084280443945353267/1195700948786491403/1195944696665604156](https://discord.com/channels/1084280443945353267/1195700948786491403/1195944696665604156)<br>
Cycle detectionという問題があるのか。おもろい。<br>
[https://en.wikipedia.org/wiki/Cycle_detection#Floyd's_tortoise_and_hare](https://en.wikipedia.org/wiki/Cycle_detection#Floyd's_tortoise_and_hare)<br>

## Step 2
**コメント集を読む**<br>
[https://docs.google.com/document/d/11HV35ADPo9QxJOpJQ24FcZvtvioli770WWdZZDaLOfg/edit?tab=t.0#heading=h.2k4z0wt6ytf9](https://docs.google.com/document/d/11HV35ADPo9QxJOpJQ24FcZvtvioli770WWdZZDaLOfg/edit?tab=t.0#heading=h.2k4z0wt6ytf9)

> setに突っ込んでいくのが本線
[https://discord.com/channels/1084280443945353267/1227073733844406343/1227513952938758206](https://discord.com/channels/1084280443945353267/1227073733844406343/1227513952938758206)

なぜset？　cycleの検出だから重複を省くというsetの利点が活きてない気がする。<br>
inによる走査はlistよりもsetの方が速いとかあるのか？<br>
$\rightarrow$ どうやらそうっぽい。<br>
>setの内部実装はdictと同じ<br>
[https://qiita.com/snhrhdt/items/2e514d4d6af983fcf6f0#%E3%82%BB%E3%83%83%E3%83%88set](https://qiita.com/snhrhdt/items/2e514d4d6af983fcf6f0#%E3%82%BB%E3%83%83%E3%83%88set)

>This type will be an unordered collection of unique values, just as a dictionary is an unordered collection of key/value pairs.<br>
[https://peps.python.org/pep-0218/#:~:text=This%20type%20will%20be%20an%20unordered%20collection%20of%20unique%20values%2C%20just%20as%20a%20dictionary%20is%20an%20unordered%20collection%20of%20key/value%20pairs.](https://peps.python.org/pep-0218/#:~:text=This%20type%20will%20be%20an%20unordered%20collection%20of%20unique%20values%2C%20just%20as%20a%20dictionary%20is%20an%20unordered%20collection%20of%20key/value%20pairs.)

なるほど。
> Remove “x” from the set. If “x” is not present, this method raises a LookupError exception.

確かにlookupという関数がある（中身は理解できてないが）。
[https://github.com/python/cpython/blob/b936ccdb6f6bd11250b4e638b6fa2c239907ca58/Objects/setobject.c#L78](https://github.com/python/cpython/blob/b936ccdb6f6bd11250b4e638b6fa2c239907ca58/Objects/setobject.c#L78)

**PRを読む**
- [https://github.com/katataku/leetcode/pull/1](https://github.com/katataku/leetcode/pull/1)
- [https://github.com/tarinaihitori/leetcode/pull/1](https://github.com/tarinaihitori/leetcode/pull/1)
- [https://github.com/Hurukawa2121/leetcode/pull/1](https://github.com/Hurukawa2121/leetcode/pull/1)
- [https://github.com/hayashi-ay/leetcode/pull/15](https://github.com/hayashi-ay/leetcode/pull/15)
- [https://github.com/huyfififi/coding-challenges/pull/12](https://github.com/huyfififi/coding-challenges/pull/12)

>`is None`派と`is not None`派
[https://github.com/tarinaihitori/leetcode/pull/1#discussion_r1802281339](https://github.com/tarinaihitori/leetcode/pull/1#discussion_r1802281339)

そこに派閥があること自体知らなかった。自分はどうだろうか？　その場その場で自分の自然言語的な「言いやすさ」を優先せいてる気がする。<br><br>

>[単純なloop detectionではなく、loopが始まる場所を返せ、との問題設定だった。](https://github.com/huyfififi/coding-challenges/pull/12/files#diff-608caf1d956844284cc460b08b749d1cb05e0b639b6338c25971f46b946c6f08:~:text=Cracking%20the%20Coding%20Interview%E3%81%AE%E8%AA%AD%E3%81%BF%E8%BF%94%E3%81%97)

考えておこう。

> 再帰で inner function ではなく、別の関数として書く
```
class Solution:
    def find_cycle(self, head, visited):
        if head is None:
            return False
        if head in visited:
            return True
        visited.add(head)
        return self.find_cycle(head.next, visited)

    def hasCycle(self, head: Optional[ListNode]) -> bool:
        return self.find_cycle(head, set())
```
確かに再帰で書ける。<br><br>

>[https://github.com/huyfififi/coding-challenges/blob/leetcode-141-linked-list-cycle/leetcode/141/step1_pigeonhole.py](https://github.com/huyfififi/coding-challenges/blob/leetcode-141-linked-list-cycle/leetcode/141/step1_pigeonhole.py)
```python
class Solution:
    def hasCycle(self, head: Optional[ListNode]) -> bool:
        counter = 1
        while head:
            if counter > 10**4:
                return True
            head = head.next
            counter += 1
        return False
```
おもしろい解法。<br><br>

うさぎと亀を考えてみる。中国の剰余定理っぽいなと思いつつ、色々ネットを見る。<br>
>When 𝑆=2 as in the problem as given, this is simply 𝑛 ≡ 0 mod 𝐶.
>[https://math.stackexchange.com/questions/913499/proof-of-floyd-cycle-chasing-tortoise-and-hare#:~:text=.-,When,.,-This%20happens%20exactly](https://math.stackexchange.com/questions/913499/proof-of-floyd-cycle-chasing-tortoise-and-hare#:~:text=.-,When,.,-This%20happens%20exactly)

なるほど。おもしろい。

```python
# Definition for singly-linked list.
# class ListNode:
#     def __init__(self, x):
#         self.val = x
#         self.next = None

class Solution:
    def hasCycle(self, head: Optional[ListNode]) -> bool:
        if head is None:
            return False

        # Floyd's tortoise and hare
        fast = head.next
        slow = head
        while fast and fast.next:
            if fast
                return True
            fast = fast.next.next
            slow = slow.next

        return False
```

>私自身はwhile slow is not fast:というループ条件にしていた

確かにこの条件もあるな。ただ自分の好みとしては「終点（＝None）」につくまでという描像があるので`while fast and fast.next:`が好きだ。



## Step 3
素直な方で解く。変数名や改行位置を整える。
```python
# Definition for singly-linked list.
# class ListNode:
#     def __init__(self, x):
#         self.val = x
#         self.next = None

class Solution:
    def hasCycle(self, head: Optional[ListNode]) -> bool:
        if not head:
            return False

        visited = set()
        current = head
        while current:
            if current in visited:
                return True
            visited.add(current)
            current = current.next

        return False
```
時間計算量: O(n), n = length of cycle<br>
$\rightarrow$ setのinはテーブルのルックアップ<br>
空間計算料: O(n)<br><br>

Floyd's tortise and hare
```python
class Solution:
    def hasCycle(self, head: Optional[ListNode]) -> bool:
        if not head:
            return False

        fast = head.next
        slow = head
        while fast and fast.next:
            if fast == slow:
                return True
            fast = fast.next.next
            slow = slow.next

        return False
```
時間計算量: O(n), n = length of cycle<br>
空間計算料: O(1)<br><br>

## 感想
Step 1でギブアップしたとき、`Definition`や型ヒントを見ていなかったのは大いに反省すべき点。「見たことない問題だ」と思って視野が狭まってしまった。<br>
cycle detectionという問題のクラス自体を初めて知った。ユースケースが思いつけていないので、Discordを漁っておこう。<br>
あとsetがlookupしてることにこれまで思い至っておらず、今回しれてよかった。<br>
