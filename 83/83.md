## Step 1
とりあえず思いついたままに書く。<br>
```python
# **Wrong answer**
# Definition for singly-linked list.
# class ListNode:
#     def __init__(self, val=0, next=None):
#         self.val = val
#         self.next = next
class Solution:
    def deleteDuplicates(self, head: Optional[ListNode]) -> Optional[ListNode]:
        node = head
        while node is not None:
            forward = node.next
            while forward is not None:
                if node.val is not forward.val:
                    node.next = forward
                    break
                forward = forward.next
            if forward is None:
                node.next = None
            node = node.next
        
        return head
```

`node.val is not forward.val`が変であることに気づく。<br>
前問でもらった「nodeの比較は`is`がいいのでは？」というコメントを盲目的に反映してしまう。反省。<br>

```python
# **Accepted one**
# Definition for singly-linked list.
# class ListNode:
#     def __init__(self, val=0, next=None):
#         self.val = val
#         self.next = next
class Solution:
    def deleteDuplicates(self, head: Optional[ListNode]) -> Optional[ListNode]:
        node = head
        while node is not None:
            forward = node.next
            while forward is not None:
                if node.val != forward.val:
                    node.next = forward
                    break
                forward = forward.next
            if forward is None:
                node.next = None
            node = node.next
        
        return head
```
時間計算量: O(n), n = the number of nodes in the list <br>
実行時間: 3 * 10^2 / 10^{-6} = 3 * 10^{-4} s<br>
空間計算量: O(1)

## Step 2
**PRを読む**
- [https://github.com/katataku/leetcode/pull/2](https://github.com/katataku/leetcode/pull/2)<br>
- [https://github.com/tarinaihitori/leetcode/pull/3](https://github.com/tarinaihitori/leetcode/pull/3)<br>
- [https://github.com/Hurukawa2121/leetcode/pull/3](https://github.com/Hurukawa2121/leetcode/pull/3)<br>
- [https://github.com/hayashi-ay/leetcode/pull/20](https://github.com/hayashi-ay/leetcode/pull/20)<br>
- [https://github.com/potrue/leetcode/pull/3](https://github.com/potrue/leetcode/pull/3)<br>

```python
# cited from https://github.com/katataku/leetcode/blob/83.-Remove-Duplicates-from-Sorted-List/83.%20Remove%20Duplicates%20from%20Sorted%20List.md
class Solution:
    def deleteDuplicates(self, head: Optional[ListNode]) -> Optional[ListNode]:
        if head is None:
            return None
        current = head
        while current.next is not None:
            if current.val == current.next.val:
                current.next = current.next.next
            else:
                current = current.next
        return head
```

あーそうか。自分はdupulicatesでなくなったら、nextをupdateしようという発想出会ったが、nextは順次updateすればいいのか。スマートだ。<br><br>

>末尾再帰

[末尾再帰による最適化](https://qiita.com/pebblip/items/cf8d3230969b2f6b3132)<br>
[https://github.com/ichika0615/arai60/pull/7#discussion_r1877624318](https://github.com/ichika0615/arai60/pull/7#discussion_r1877624318)<br>
なるほど。return後に計算がなければスタックを積むのではなく、スタックのラベルを付け替えれば良い。ただしその代わりに、引数をうまく更新をするってことね。賢いトリック。<br>
感覚としては
```
f(x, y) = xyとして
n! = f(n, (n-1)!) = f(n*(n-1), (n-2)!)
```
としてる感じか。なんか前にぼんやりと聞いたカリー化と似てるな。<br><br>

pythonでは末尾再帰形式で書いてもいつもスタックが節約されるとは限らない。<br>
>このようにスタック領域が消費されないのは、Pythonの関数がPythonの関数を呼び出した場合に限られる、という点に注意してください。Pythonでは多くの関数や型がC言語で実装されて組み込まれており、こういった関数や型のメソッドを呼び出すとスタックを消費します。<br>
>[https://www.python.jp/news/wnpython311/inline-function.html#:~:text=%E3%81%93%E3%81...](https://www.python.jp/news/wnpython311/inline-function.html#:~:text=%E3%81%93%E3%81%AE%E3%82%88%E3%81%86%E3%81%AB%E3%82%B9%E3%82%BF%E3%83%83%E3%82%AF%E9%A0%98%E5%9F%9F%E3%81%8C%E6%B6%88%E8%B2%BB%E3%81%95%E3%82%8C%E3%81%AA%E3%81%84%E3%81%AE%E3%81%AF%E3%80%81Python%E3%81%AE%E9%96%A2%E6%95%B0%E3%81%8CPython%E3%81%AE%E9%96%A2%E6%95%B0%E3%82%92%E5%91%BC%E3%81%B3%E5%87%BA%E3%81%97%E3%81%9F%E5%A0%B4%E5%90%88%E3%81%AB%E9%99%90%E3%82%89%E3%82%8C%E3%82%8B%E3%80%81%E3%81%A8%E3%81%84%E3%81%86%E7%82%B9%E3%81%AB%E6%B3%A8%E6%84%8F%E3%81%97%E3%81%A6%E3%81%8F%E3%81%A0%E3%81%95%E3%81%84%E3%80%82Python%E3%81%A7%E3%81%AF%E5%A4%9A%E3%81%8F%E3%81%AE%E9%96%A2%E6%95%B0%E3%82%84%E5%9E%8B%E3%81%8CC%E8%A8%80%E8%AA%9E%E3%81%A7%E5%AE%9F%E8%A3%85%E3%81%95%E3%82%8C%E3%81%A6%E7%B5%84%E3%81%BF%E8%BE%BC%E3%81%BE%E3%82%8C%E3%81%A6%E3%81%8A%E3%82%8A%E3%80%81%E3%81%93%E3%81%86%E3%81%84%E3%81%A3%E3%81%9F%E9%96%A2%E6%95%B0%E3%82%84%E5%9E%8B%E3%81%AE%E3%83%A1%E3%82%BD%E3%83%83%E3%83%89%E3%82%92%E5%91%BC%E3%81%B3%E5%87%BA%E3%81%99%E3%81%A8%E3%82%B9%E3%82%BF%E3%83%83%E3%82%AF%E3%82%92%E6%B6%88%E8%B2%BB%E3%81%97%E3%81%BE%E3%81%99%E3%80%82)<br>

📝メモ：match文, 竹内関数<br>
[https://docs.python.org/ja/3.10/reference/compound_stmts.html#the-match-statement](https://docs.python.org/ja/3.10/reference/compound_stmts.html#the-match-statement)<br>
[https://ja.wikipedia.org/wiki/%E7%AB%B9%E5%86%85%E9%96%A2%E6%95%B0](https://ja.wikipedia.org/wiki/%E7%AB%B9%E5%86%85%E9%96%A2%E6%95%B0)<br><br>

**コメント集を読む**<br>
[https://docs.google.com/document/d/11HV35ADPo9QxJOpJQ24FcZvtvioli770WWdZZDaLOfg/edit?tab=t.0#heading=h.voz9njv1gtqy](https://docs.google.com/document/d/11HV35ADPo9QxJOpJQ24FcZvtvioli770WWdZZDaLOfg/edit?tab=t.0#heading=h.voz9njv1gtqy)<br>
```python
# cited from https://discord.com/channels/1084280443945353267/1195700948786491403/1196399353116499970
curr = head
while curr:
    while curr.next and curr.val ==  curr.next.val:
        curr.next = curr.next.next
    curr = curr.next
return head
```
確かにif文は省略できる。<br><br>

>None かどうかを判定する場合は、 if head is None: と書くことをお勧めいたします<br>
[https://github.com/atomina1/Arai60_review/pull/2#discussion_r1890231175](https://github.com/atomina1/Arai60_review/pull/2#discussion_r1890231175)

「次がNoneでなく、node.valが変わるまで、nextをアップデートする」というwhileの条件が気に入った。「node.valが変わるまで、ただし次がNoneでない間、nextをアップデートする」の方が自然かと思って書き換える<br>
$\rightarrow$ 通らない。

```python
# **Wrong answer**
# Definition for singly-linked list.
# class ListNode:
#     def __init__(self, val=0, next=None):
#         self.val = val
#         self.next = next
class Solution:
    def deleteDuplicates(self, head: Optional[ListNode]) -> Optional[ListNode]:
        node = head
        while node is not None:
            while node.val == node.next.val and node.next is not None:
                node.next = node.next.next
            node = node.next
        
        return node

# Error message
# AttributeError: 'NoneType' object has no attribute 'val'
#                       ^^^^^^^^^^^^^
#     while node.val == node.next.val and node.next is not None:
# Line 10 in deleteDuplicates (Solution.py)
#           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
#     ret = Solution().deleteDuplicates(param_1)
# Line 39 in _driver (Solution.py)
#     _driver()
# Line 54 in <module> (Solution.py)
```

`node.next`に対する`None`チェックが先である必要がある。`None`でないから初めてチェックできる。
「次がNoneでなく、node.valが変わるまで、nextをアップデートする」<br><br>
$\downarrow$　翻訳はこう<br><br>
「次がNoneでない場合で、node.valが同じである間は、nextをアップデートする」

## Step 3
```python
# class ListNode:
#     def __init__(self, val=0, next=None):
#         self.val = val
#         self.next = next
class Solution:
    def deleteDuplicates(self, head: Optional[ListNode]) -> Optional[ListNode]:
        node = head
        while node is not None:
            while node.next is not None and node.val == node.next.val:
                node.next = node.next.next
            node = node.next

        return head
```

最初のやつのリバイズ

```python
class Solution:
    def deleteDuplicates(self, head: Optional[ListNode]) -> Optional[ListNode]:
        node = head
        while node is not None:
            forward = node.next
            while forward is not None and node.val == forward.val:
                forward = forward.next
            node.next = forward
            node = node.next

        return head
```

## 感想
いつも最適化されるとは限らないけど、pythonでもできるだけ末尾再帰で書く方がいいかも？
