## Step 1
とりあえず思いついたまま書く。
```
# Definition for singly-linked list.
# class ListNode:
#     def __init__(self, val=0, next=None):
#         self.val = val
#         self.next = next
class Solution:
    def update_link_to_next(node: ListNode): -> ListNode:
            forward = node.next
            while forward is not None:
                if node.val is not forward.val:
                    node.next = forward
                    break
                forward = forward.next
            if fowwrd is None:
                node.next = None

        return node

    def deleteDuplicates(self, head: Optional[ListNode]) -> Optional[ListNode]:
        node = head
        while node is not None:
            node = update_link_to_next(node)
            node = node.next

        return head
```

## Step 2
## Step 3
## 感想
